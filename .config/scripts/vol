#/bin/bash

# p r i m i t i v e
mute=`pamixer --get-mute`
if [ $mute = true ] ; then
  echo "Muted"
else
  vol=`pamixer --get-volume`
  echo $vol
  for i in {5..100..5} ; do
    if [ $i -le $vol ] ; then
      echo -n ◼
    else
      echo -n ◻
    fi
  done
fi

